---
title: "【中字】海外博主反驳AI编程主流：Rust才是最佳选择"
url: "https://www.bilibili.com/video/BV17DGQ6VEVP/?spm_id_from=333.1007.top_right_bar_window_history.content.click&vd_source=0911033b6b76b290cabaf109e42e694a"
bvid: "BV17DGQ6VEVP"
cid: "38662965149"
author: "枫叶边城"
upload_date: "2026-05-28"
subtitle_lang: "中文"
created: "2026-06-14"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=116649566931689&bvid=BV17DGQ6VEVP&cid=38662965149&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 字幕

`00:14` 我的名字是丹尼尔·祖克
`00:15` 我是Sentry的Rust SDK维护者
`00:19` 我想告诉大家为什么我认为Rust是振动编码的理想语言
`00:25` 关于编码或振动编码该使用哪种语言的常规观点
`00:33` 无论你如何称呼它
`00:36` Rust可能不是你首先想到的选择
`00:41` 你知道的
`00:41` 也许你以为自己知道
`00:44` 可能ChatGPT也有个好主意
`00:46` 哪种是最佳的编码语言
`00:48` 鉴于它也是一种代理类型
`00:52` 它会告诉你没有单一的最优语言
`00:56` 但Python大概位列榜首
`01:00` 并且支持率很高
`01:03` 当我询问时它提到JavaScript和TypeScript
`01:06` 而我认为这
`01:08` 至少在我的经验中相当准确
`01:12` 尽管我会调整顺序
`01:13` 因为
`01:14` TypeScript似乎已成为近期首选
`01:22` 因此GitHub上有一篇新文章
`01:28` 大概去年末发布
`01:29` 文中称AI认为AI推动了TypeScript登顶
`01:36` 至少从贡献者数量看
`01:39` 嗯
`01:40` 所以他们知道TypeScript位居第一
`01:44` 并强烈怀疑这源于AI辅助开发的使用
`01:52` 但为何这些语言
`01:55` Python、TypeScript
`01:56` JavaScript
`01:57` 如此适合振动编码
`02:00` 嗯
`02:01` 至少在常规观点中
`02:05` 首先
`02:07` 它们是常见熟悉的语言
`02:11` 所以它们
`02:12` 它们
`02:12` 通常是编程新手学习的第一语言
`02:17` 对人类友好
`02:19` 同时对LLM也很友好
`02:22` 还有大量框架
`02:24` 库和示例资源
`02:26` 嗯
`02:27` 这对从零构建新项目很有帮助
`02:31` 当然也可以基于现有工具开发
`02:34` 这对人类开发者有帮助
`02:37` 也对代理系统有帮助
`02:40` 它们能快速搭建并运行动态语言
`02:44` 它们被解释执行
`02:46` 至少JavaScript和Python是TypeScript
`02:49` 也许可以将某些代码编译成JavaScript之类的东西
`02:53` 但只需运行看看效果其实很简单
`02:56` 然后在此基础上迭代优化
`02:58` 嗯
`03:00` 特别是对于代理程序
`03:01` 类型支持非常有帮助
`03:03` 防止代理错误使用类型
`03:06` 嗯
`03:07` 不过确实
`03:08` 存在any类型这种问题
`03:12` 在TypeScript和类型化Python中也有类似情况
`03:16` 但总体而言
`03:18` 这些语言
`03:20` LLMs
`03:21` 模型本身在首次尝试时就能输出可运行代码
`03:27` 因为这些语言简单且约束较少
`03:33` 所以我认为
`03:35` 由于LLMs似乎擅长编写这类代码
`03:40` 人们纷纷转向这些语言
`03:42` 嗯
`03:43` 但根据我的经验，很多人很少质疑
`03:48` 是否应该优先优化这一点
`03:56` 传统编程语言对模型来说容易编写
`04:00` 但这是否是好事呢
`04:04` 我的观点是，模型易于编写的重要性被高估了
`04:10` 事实如此
`04:13` 甚至可以说
`04:14` 在某些情况下，模型易于编写反而不好
`04:21` 嗯
`04:22` 语言的动态和灵活性
`04:26` 使得代理或LLM更容易编写
`04:30` 应该说编写JavaScript
`04:32` Python、TypeScript
`04:33` 嗯
`04:35` 但这种灵活性也极易导致错误
`04:40` 有时甚至明显错误
`04:42` 有时则是隐性错误
`04:45` 添加类型约束很有帮助
`04:48` 但这只能解决部分问题
`04:50` 因为仅提供类型安全
`04:53` 而且类型安全并不强
`04:56` 嗯
`04:57` 在TypeScript或Python中
`04:58` 当然这也是问题
`05:03` 因为LLMs本质上是不可靠的系统
`05:09` 未来它们会减少错误发生
`05:14` 但这种情况不会完全消失
`05:19` 就像最聪明的人也会犯错
`05:23` 我们需要防范人为错误
`05:26` 同样需要防范LLM错误
`05:31` 人们通常会通过这种方式
`05:33` 尤其是在传统的编程语言中，常用的方法是添加测试用例
`05:40` 这非常有帮助
`05:42` 但仅依赖测试和代码审查工具存在很多问题
`05:50` 首先
`05:53` 你知道的
`05:53` 如果你没有巧妙引导AI
`05:56` 它通常会在实现代码后编写测试
`05:58` 结果只是测试实现细节而非实际行为
`06:06` 即使采用测试驱动开发
`06:08` 但测试通常只能在失败时证明错误
`06:14` 因为无法测试所有可能的输入组合
`06:21` 在很多情况下无法证明所有输入都产生正确输出
`06:28` 当然还有
`06:29` 如果由LLM生成测试用例
`06:32` 它们在编写测试时可能会出错
`06:35` 同样的问题也适用于代码审查工具
`06:39` 更从哲学层面来看
`06:42` 没错
`06:42` 我们都知道AI代表人工智能
`06:46` 但有一本名为《奇点临近》的书
`06:49` 我最近读过
`06:50` 还没读过的人都强烈推荐
`06:53` 这本书的作者是
`06:55` 尤瓦尔·赫拉利
`06:56` 哈拉里
`06:57` 他是一位历史学家
`06:58` 对人工智能有独特的视角
`07:02` 他在书中探讨人类信息
`07:06` 从石器时代到印刷术
`07:11` 再到互联网
`07:13` 直至现在的LLM时代
`07:14` 没错
`07:15` 嗯
`07:16` 他认为LLM非常独特
`07:18` 因为这是首次出现非人类
`07:22` 能够生成人类语言的技术
`07:25` 他提到的这一点让我印象深刻
`07:29` 是他不喜欢
`07:32` AI中的'A'是人工的
`07:35` 因为它低估了
`07:37` 嗯
`07:38` LLM和其他AI技术与人类思维的本质差异
`07:42` 他更倾向于称之为外星智能
`07:47` 嗯
`07:49` 因为它们底层的思维机制与人类完全不同
`07:55` LLM预测连续输入的标记
`07:59` 这是一种强大的思考机制
`08:02` 但并非人类的思维方式
`08:04` 我的观点是，其失败模式可能完全超出我们的预期
`08:09` 如果你曾尝试过与AI进行编码
`08:13` 你可能遇到过这样的情况：生成的代码看起来非常整洁
`08:17` 变量名可能很合理
`08:20` 注释也很到位等等
`08:21` 但当你仔细查看时
`08:23` 可能会发现哪里不对
`08:24` 比如可能存在细微的bug
`08:26` 或者依赖某些启发式方法
`08:29` 而你本可以更可靠、更轻松地检查实际内容
`08:34` 在某些情况下
`08:36` 因此在使用LLM和基于代理的开发时必须格外谨慎
`08:46` 这让我想到墨菲定律
`08:48` 它基本说明任何可能出错的事最终都会出错
`08:52` 迟早会在某个时候发生
`08:53` 所以如果你使用没有确定性保障的语言
`08:58` 即使进行人工审核
`09:01` 审核测试
`09:04` 良好的测试流程
`09:06` 如果没有绝对确定性的防护机制
`09:12` 最终难免会出现故障
`09:14` 而在像JavaScript、Python、TypeScript这类语言中
`09:16` Python、TypeScript
`09:18` 缺乏这些防护机制
`09:20` 故障发生的频率会更高
`09:24` 没错
`09:27` 这引出了Rust语言
`09:30` 这是一种具有多重约束的语言
`09:33` 对于不了解Rust的听众
`09:38` 或了解不多的听众
`09:40` 先提供一些背景
`09:42` 它是一种编译型语言
`09:43` 以安全性和性能为核心设计
`09:47` 力求达到C和C++的运行速度
`09:50` 但确保内存安全
`09:52` 类型安全
`09:53` 基本上希望
`09:58` 拥有如此严格的编译器
`10:00` 如果代码能通过编译
`10:02` 你可以相对确定
`10:05` 代码中大部分类型的错误已被排除
`10:10` 这是因为编译器强制执行不变量如类型安全
`10:16` 内存
`10:17` 安全并发
`10:19` 等等
`10:21` 语言还力求对新手友好
`10:24` Rust本身
`10:26` 未接触过的人可能认为它非常复杂
`10:29` 但团队努力让语言易于学习
`10:32` 编译器错误信息能详细说明问题所在
`10:35` 提供大量关于错误原因的信息
`10:38` 那么如何解决问题
`10:40` 因此他们提供了大量上下文
`10:43` 当然，当AI代理编译Rust代码时这非常有帮助
`10:48` 遇到错误然后需要修复它
`10:52` 正如我之前提到的
`10:53` Rust有很多安全保证
`10:56` 首先值得提到的是类型安全非常严格
`11:02` 你无法通过任意类型或未检查的转换来绕过它
`11:07` 空安全是另一个重要特性
`11:10` 如果你来自其他语言
`11:12` 没有通用的空值
`11:14` 如果你想有一个可以为空的类型选项
`11:19` 你需要显式定义为可选类型
`11:22` 编译器会强制你在访问前始终检查值是否存在
`11:28` 内部值与无畏并发
`11:32` 我认为这非常强大
`11:35` 这意味着Rust编译器会检查
`11:40` 如果你有任何多线程代码，任何线程间共享的数据
`11:46` 这些操作都以线程安全的方式完成
`11:49` 这只是一个简短的列表
`11:51` Rust编译器强制执行的内容还有很多
`11:57` 但我想快速给大家展示无畏并发
`12:03` 因为我觉得这非常强大
`12:06` 这里有一个小代码示例
`12:08` 这里有一个计数器
`12:12` 它将从零值开始
`12:14` 我们将创建一百个线程
`12:17` 每次都会取计数器并增加内部值
`12:26` 所有线程完成后
`12:28` 现在你应该期望这个值变为一百
`12:33` 这里有个问题
`12:34` 那就是嗯
`12:36` 这些类型
`12:38` 它们是为嗯
`12:40` 用于共享可变数据
`12:42` 但仅限于单线程内
`12:44` 它们没有同步机制
`12:48` 不支持多线程安全访问
`12:51` 嗯
`12:51` 所以在像TypeScript这样的语言中
`12:55` 可能会像这样编译通过
`12:57` 它可能会运行
`12:59` 然后你才会发现这个问题
`13:01` 当偶尔从这里得到的值不是一百时
`13:06` 这可能会发生
`13:07` 尤其是当这是更大应用中的一个小部分时
`13:11` 很难调试数据竞争发生的位置
`13:17` 但在Rust中这根本无法编译
`13:21` 你会收到编译错误
`13:22` 并提示错误：未来无法安全地在不同线程间传递
`13:27` 嗯
`13:28` 这个未来
`13:29` 所以这里的这个小异步块
`13:33` 嗯，这不是发送的
`13:35` 所有发送意味着可以在线程间安全传递，而且它并不如此
`13:41` 这个错误信息不够有用
`13:43` 但如果你在错误信息中向下滚动
`13:45` 它会进一步解释
`13:47` 而这正是对你的AI代理真正有帮助的
`13:51` 因为它会说
`13:52` 哦
`13:53` 这里的值
`13:54` 被捕获的计数器值
`13:57` 它不是发送的
`13:58` 它的类型是rc引用单元
`14:01` 我三二
`14:02` 而这也不是发送的
`14:04` 所以如果你的AI代理
`14:06` 当它编译你的项目时
`14:09` 会得到这个编译错误
`14:12` 它可以立即修改为线程安全类型
`14:17` 而Rust中有很多这样的类型
`14:23` 当然这些约束都有权衡
`14:27` Rust对LLM来说更难一次正确实现
`14:31` 因为需要遵循许多规则
`14:35` 但我认为这是好事
`14:37` 嗯
`14:38` 这是因为不仅LLM在写代码
`14:41` 我们将LLM放入AI代理中
`14:44` 它处于循环中
`14:45` 可以自主执行任务
`14:47` AI代理非常适合编译代码
`14:52` 检查任何失败
`14:53` 然后去修复它们
`14:56` 每一个编译错误
`14:58` 嗯，可能是你在生产代码中避免的漏洞
`15:03` 所以嗯，关于Rust编译器之类的东西
`15:09` 我听说有时人们抱怨编译时间慢
`15:13` 但可以肯定的是，它比让AI代理审查代码更快
`15:18` 而且可能无法发现所有错误
`15:22` 而Rust编译器能保证找到的错误
`15:25` 我仍然认为你应该使用它
`15:27` 当然，但拥有这个额外的安全层很好
`15:33` 我想
`15:36` 当然这是赞助演讲，我是Sentry的
`15:40` 所以这是小广告页
`15:43` 嗯
`15:44` 你应该试试我们
`15:45` 如果你还没试过
`15:46` 这个二维码将免费提供三个月的企业计划
`15:52` 嗯
`15:52` 我们有代理监控功能
`15:54` 我们楼下有个展台
`15:56` 过来看看吧
`15:56` 欢迎随时询问关于Sentry的问题
`16:00` 或者如果你想和我聊聊这个演讲
`16:02` 你也可以过来
`16:03` 我很乐意聊天
`16:04` 谢谢