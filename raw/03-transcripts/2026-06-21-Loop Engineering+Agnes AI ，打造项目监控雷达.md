---
title: "Loop Engineering+Agnes AI ，打造项目监控雷达"
url: "https://www.bilibili.com/video/BV1G7jJ6nEQJ/?spm_id_from=333.1391.0.0&vd_source=0911033b6b76b290cabaf109e42e694a"
bvid: "BV1G7jJ6nEQJ"
cid: "39265831212"
author: "第四种黑猩猩CHIMP"
upload_date: "2026-06-20"
subtitle_lang: "中文"
created: "2026-06-21"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=116782207602812&bvid=BV1G7jJ6nEQJ&cid=39265831212&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 字幕

`00:00` CLCODE负责人BORIS斯啊
`00:01` 最近一直在提一个新的词
`00:02` Loop engineering
`00:03` 循环工程
`00:04` 第一眼看到之后
`00:05` 我以为又是圈在发明一些黑化
`00:07` 研究了一圈之后呢
`00:07` 发现其实挺简单的
`00:09` 就是你别再手动
`00:10` 一条一条去给你的agent写提示词了
`00:13` 你设计一个循环
`00:14` 让agent ent自己去定时发现任务
`00:16` 处理任务记录结果
`00:17` 然后agent他自己去写下一轮的提示词接着跑
`00:20` 所以呢我也做了一个实验
`00:21` 顺便给大家展示一下什么叫做loop engineering
`00:24` 然后怎么去跑这个提示词工程
`00:26` 比如说啊
`00:27` 我想让agent每天去自动扫描这个GITHUB的trading
`00:30` 自己去虫项目打分
`00:32` 下载项目
`00:33` 尝试安装运行
`00:34` 然后最后发一条微信给我说
`00:36` 今天哪些项目值得看
`00:37` 然后哪些项目只是看着火
`00:39` 但没什么用
`00:40` 整个系统啊其实挺简单的
`00:41` 就一份提示词
`00:42` 一份历史记录
`00:43` 几个脚本
`00:44` 但他每天都能自己跑起来
`00:45` 像一个不会休息的项目
`00:47` 雷达这种任务呢其实长期来跑
`00:49` 非常消耗token的
`00:50` 所以呢
`00:50` 我一直想找一个长期适合使用的这个模型
`00:53` 后来我发现了一款很不错的
`00:55` 全模态的免费大模型
`00:56` Agnes 2.0flash
`00:58` 现在这套任务啊
`00:59` 我主要就是用这个免费大模型来跑的
`01:01` 我们先来认识一下这个AGN
`01:02` AGNEAI呢是一家做多模态基础模型的公司
`01:05` 文本图片视频三个模型呢全部都免费开放
`01:09` 不限期也不用绑这个信用卡
`01:11` 它有多火呢
`01:12` 免费开放到现在
`01:13` 全模态的总调用量超过了3.12万亿token
`01:16` 其中的文本模型呢AGENNIS2.0flash啊
`01:19` 贡献了其中大概1.9万亿
`01:21` 说明啊真的有大量的开发者在拿它去干活
`01:23` 我今天这套workflow呢主要就是用的这个文本模型
`01:26` 我们先来看一下这套东西是怎么搭起来的
`01:29` 我们在这个vs code里面打开这个项目
`01:31` 一个呢是这个prompt m d里面写清楚
`01:34` agent每次运行都要做什么
`01:36` 是一个这个固定的流程
`01:37` 第二个呢是这个sin点TXT
`01:40` 一个轻量的驱虫的索引
`01:42` 然后每行只记一个项目名
`01:43` 仓库名
`01:44` 日期分数有没有通知过
`01:46` 就这么几个字段不占上下文
`01:48` 第三个呢是这个history点记录高分项目的详细状态
`01:52` 评分跳过的理由
`01:54` 运行的结果
`01:55` 第四个呢是每天一份的报告
`01:56` reports目录
`01:57` 下面按照日期去保存
`01:59` 保存当天完整的这个分析
`02:01` 第五个呢就是工作目录workspace
`02:03` 临时chrome项目和跑项目的这个地方
`02:05` 剩下的就是这两个脚本
`02:07` 一个呢是抓trading的这样一个脚本
`02:10` 另外一个呢是发微信通知的这样一个脚
`02:12` 那为什么这样设计呢
`02:13` 里面的核心思想就是啊代码做稳定工具
`02:16` markdown保存状态
`02:17` agent负责判断和执行
`02:19` 来举几个具体的例子啊
`02:20` 比如说GITHUB项目抓取啊
`02:21` 每次都会读到重复的项目
`02:23` 需要去虫
`02:24` 我没有交给agent
`02:25` 而是交给了脚本
`02:26` 为什么呢
`02:26` 因为去虫本质上就是这个集合的运算
`02:29` 是个确定性的操作
`02:30` 让agent来判断又贵又慢
`02:32` 还容易出错
`02:33` 脚本呢直接去读sing点TXT就行了
`02:35` 可以把里面重复的项目直接过滤掉
`02:37` 又干净又稳状态
`02:39` 我分成两层
`02:40` thing点TXT的只存项目标识
`02:42` 详细的理由和结果
`02:44` 每次追加在history点MD和和报告里面
`02:47` 这样不会因为天天累积跳过的项目啊
`02:49` 上下文窗口越来越长
`02:50` 抓trading驱虫项目
`02:52` 发微信这种确定性的操作啊
`02:54` 全部交给脚本
`02:55` 比每次让agent ent临时去想各种办法操作
`02:58` 要靠谱的多
`02:59` 然后我们接下来详细来看一下
`03:00` 主要的这个PROMPP点MD
`03:02` 来看一下AINT每天具体要干什么
`03:04` 第一步就是跑这个抓取脚本返回的呢
`03:07` 就是去重过的全新的项目列表
`03:09` 第二步呢就是按我的偏好去轻量打分
`03:12` 我设计了这几个维度
`03:14` 第一个呢就是是否和AI和开发工具相关
`03:16` 然后有没有明显的新意
`03:18` dot的增长是不是异常
`03:19` Read me
`03:20` 有没有把问题讲清楚
`03:21` 适不适合做短视频的选题
`03:23` 第三步啊
`03:24` 就是怎么处理低分的项目
`03:25` 低分的项目呢就直接写进SC点TXT和这个history点MD
`03:29` 里面只写一句跳过的理由
`03:31` 第四步啊
`03:32` 高分的项目怎么处理
`03:33` 这个会稍微复杂一点
`03:34` 我呢会派一个子agent去深度的分析
`03:36` 最后呢把当天高峰的项目整理成摘要
`03:39` 给我发微信通知
`03:40` 这里呢要插一句AGNES的特点啊
`03:42` 这个流程里面呢agent特要反复的读read me
`03:45` 也许日志还有历史的markdown
`03:47` 特别呢吃上下文AGONES
`03:49` 2.0flash的100万上下文窗口啊
`03:51` 现在正在灰度测试
`03:52` 加上它是个免费的大模型
`03:54` 正好呢适合这种长跑的后台任务
`03:56` 高分项目具体怎么处理啊
`03:57` 我们再来详细看一下the agent
`03:59` 拿到一个项目之后呢
`04:00` 先把这个项目克隆下来
`04:02` 读read me
`04:02` 依赖文件配置和issue
`04:05` 搞清楚他是干什么的
`04:06` 怎么启动的
`04:07` 然后呢就是安装就安装
`04:08` 能跑就跑
`04:09` 能测就测
`04:10` 如果跑失败了
`04:11` 接下来关键的报错
`04:12` 如果跑成功了
`04:13` 接下来这个启动的命令跟实际最终的效果
`04:16` 然后最后呢做一个项目分析
`04:17` 他这个项目是干什么的
`04:19` 为什么这么火
`04:19` 有没有技术含量
`04:20` 有没有跑通
`04:21` 适不适合我来做视频
`04:23` 那这里啊我为什么单独要派一个子agent
`04:25` 而不是让主agent自己上呢
`04:27` 因为跑陌生项目会产生一大堆的脏数据
`04:30` 安装日志啊
`04:31` 然后报错的堆栈啊
`04:32` 测试输出啊
`04:33` 这些东西呢全在子agent自己的上下文里面
`04:35` 主agent只接收干净的最后的分析的结果
`04:38` 这样的主agent他的上下文始终是清爽的
`04:41` 对于单个项目来说
`04:42` 就算你跑挂了
`04:43` 然后跑不通产生了很多这个垃圾
`04:45` 也只影响那一个子agent
`04:47` 然后来说一下具体的开发过程
`04:48` AGNE走呢是open i的chat completions这个格式
`04:52` 所以啊我用cc switch本地路由功能
`04:54` 把AGONNE2.0flash下直接可以接近了
`04:56` 进入cc switch设置路由
`04:59` 本地路由开启显示开关总开关
`05:02` 选择cloud路由
`05:03` 然后回到首页去添加这里的cloud的配置
`05:07` 添加配置
`05:08` 然后呢我们就正常添加cloud code配置
`05:10` 填上这个请求的URL
`05:12` 填上你的APIKE
`05:13` 然后模型呢全部都选AGNES2.0flash
`05:16` 接好之后啊
`05:17` 我就可以让cloud code
`05:18` 自己把这头flow的目标结构搭出来
`05:21` 写好那几份项目的MD跟脚本了
`05:23` 我先开发的是抓取脚本
`05:25` 测试它能不能正常的去抓取github training
`05:28` 并且驱虫
`05:28` 然后再开发的呢就是微信通知的这个脚本
`05:32` notify点PY
`05:33` 我用的呢是server fan的微信服务号
`05:35` 特别适合这种单向通知
`05:37` 把他的文档丢进这个开发目录里面
`05:39` 然后直接就让agent去开发就好了
`05:41` 测下来呢也能够正常的收到微信的通知
`05:44` 然后最后呢来跑一次主agent
`05:46` 特让他从项目抓取到发微信完整的跑一遍
`05:49` 这里再插一句
`05:50` 我体会到的AGONNE的特点
`05:51` 它的RPM呢大概是每分钟20次请求
`05:54` 对个人来说我就完全就够用了
`05:56` 全部跑完之后呢
`05:57` 我们来看具体的结果
`05:58` 打开history点MD啊
`05:59` 能看到哪些项目分析过
`06:01` 哪些被跳过
`06:02` 为什么跳过
`06:03` 打开当天的报告是完整的项目分析
`06:05` 打开workspace
`06:06` 高分项目被克隆下来
`06:07` 里面有他尝试过的运行过的痕迹
`06:10` 终端的日志里面安装运行失败还是成功的
`06:13` 这个过程都保留着
`06:15` 最后呢是微信通知
`06:16` 我只让他推送最值得看的3~5个项目
`06:19` 一条消息呢大概长这样子
`06:20` 今天值得看的GITHUB热门项目
`06:22` 这个项目已经成功
`06:23` 适合深挖
`06:24` 这个项目呢有点意思
`06:25` 但demo跑失败了
`06:26` 这个项目呢适合做一期短视频
`06:27` 最后呢我来跟大家说一些我自己的看法
`06:30` 我觉得免费模型最适合的就是跑
`06:32` 这种长期重复的上下文
`06:33` 又多人工做起来又很烦的任务
`06:35` GITHUB热门项目雷达只是一个很小的例子
`06:38` 同样的思路呢你还可以去做论文雷达
`06:40` 竞品监控issue
`06:42` 除了文本模型AGONNE
`06:44` 图片模型支持4K的输出
`06:45` 然后他的视频模型呢也支持纹身
`06:48` 视频图声
`06:49` 视频音画同出
`06:50` 720P和1080P也全部都免费
`06:53` 这说实话
`06:53` 市面上免费的图片和视频生成的API几乎都没有
`06:56` 感兴趣的大家也可以去自己试一试好了
`06:59` 今天视频就到这里
`06:59` 我是地砖黑镜李超
`07:00` 我们下次见